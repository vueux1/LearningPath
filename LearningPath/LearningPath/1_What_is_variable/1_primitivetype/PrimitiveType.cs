namespace LearningPath.What_is_variable;

public class PrimitiveType {
    
   /*
    English
    Primitive types are types whose values are stored on the stack.
    For example, such as long, int, and double. 
    When no value is provided for primitive types, their default values are as follows; 0 for long, 0 for int, and 0.0 for double.
    
    Turkish
    Değerleri stack (yığıt) üzerinde tutulan ilkel tiplerdir. Örneğin; long, int ve double gibi.
    İlkel tiplere değer girilmediğinde varsayılan değerleri aşağıdaki gibidir;
    long için 0, int için 0, double içinse 0.0 dır.
    */
   
   // Eğer bir değişkenin primitive olduğunu anlamak istiyorsak aşağıdaki komutu kullanabiliriz.
   // If we want to understand if a variable is primitive, we can use the following command.
           
              // Console.WriteLine(typeof(int).IsPrimitive);
           
}