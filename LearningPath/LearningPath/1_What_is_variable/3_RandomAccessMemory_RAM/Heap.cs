namespace LearningPath.What_is_variable._3_RandomAccessMemory_RAM;

public class Heap
{
    /*
     Turkish
    Heap; Dinamik olarak nesnelerin tuttulduğu bölümdür. Method execution time’dan daha uzun süreli ihtiyacımız olan nesnelerin tutulması için uygundur.

    Genelde reference type’lar heap’te, value type’lar ise stack’te tutulur gibi bir düşünce olabiliyor. Bu sık yapılan yanlış olarak mülakatlarda başımızı belaya sokuyor. 😇

    Heap’te ne gibi durumlarda value type tutulabileceğinin örneğinide aşağıdadır.

    C# heap alanını kullandığı bazı durumlar;

    Reference Types
    Boxed Value Types
    Static Value Types
    Value Types with using ref keyword
    Value Types which is in class
    
    English
    The Heap is a section where objects are dynamically stored. It is suitable for storing objects that we need for a longer duration than the method execution time.

    There's a common misconception that reference types are stored in the heap and value types are stored in the stack. This misconception can cause trouble during interviews. 😇

    Below are examples of situations where value types can be stored in the heap.

    Some situations where C# uses the heap area include:

    Reference Types
    Boxed Value Types
    Static Value Types
    Value Types with using the ref keyword
    Value Types which are in a class
     */
}