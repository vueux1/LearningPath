namespace LearningPath.Operators;

public class assign {
    // =(atama) operatörü bir değişkene herhangi bir değer atamak için kullanılır. / The =(assignment) operator is used to assign any value to a variable.

    //*=, /=, +=, -=, &=, ^=, |= gibi operatörlere işlemli atama operatörü denir. Kullanımı şu şekildedir;
    // Operators like these are called compound assignment operators. Their usage is as follows:

    //a=a+b -> a+=b

    //a=a/b -> a/=b

    //a=a^b -> a^=b

    //Atama ve işlemli atama operatörleri aynı satırda yer alıyorsa öncelik sıralamaları soldan sağa eşittir fakat genel olarak bakıldığında bu operatörler öncelik sıralaması bakımından en son sırada yer alır.
    // If the assignment and compound assignment operators are on the same line, their priority order is equal from left to right, but generally these operators are at the bottom of the priority order.
}